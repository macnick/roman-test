require 'rails_helper'

RSpec.describe Api::V1::PostsController, type: :controller do
  # it "returns a max of 100 posts per page" do
  #   expect(Post).to receive(:limit).with(5).and_call_original
    
  #   get :index, params: { limit: 999 }
  # end

end